<h1>Welcome, <%= current_applicant.name %></h1>
<%= link_to "Log out", destroy_applicant_session_path, method: :delete, class: "btn btn-xs btn-danger" %>

<h3>These are all the employers logged in our system</h3>
<div>
  <% @all_employers.each do |employer| %>
    <p>
      <span><%= employer.name %></span>
      <%= apply_link(employer) %>
    </p>
  <% end %>
</div>
