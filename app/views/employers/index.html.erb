<h1>Welcome, <%= current_employer.name %></h1>
<%= link_to "Log out", destroy_employer_session_path, method: :delete, class: "btn btn-xs btn-danger" %>

<h3>These applicants have applied</h3>
<div>
  <% @all_applicants.each do |applicant| %>
    <span><%= applicant.email %></span>
  <% end %>
</div>
